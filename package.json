{
  "name": "astro-addons",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "lint": "eslint . --fix",
    "prepublishOnly": "pnpm -F './packages/**' build",
    "pkg:publish": "pnpm -F './packages/**' publish",
    "release": "bumpp package.json packages/*/package.json && pnpm pkg:publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^0.27.0",
    "@typescript-eslint/eslint-plugin": "^5.38.0",
    "bumpp": "^8.2.1",
    "eslint": "^8.24.0",
    "typescript": "^4.8.3"
  }
}
